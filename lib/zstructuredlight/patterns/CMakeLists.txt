file(GLOB subdirs RELATIVE ${CMAKE_CURRENT_LIST_DIR} *)
foreach(subdir ${subdirs})
    if(EXISTS ${CMAKE_CURRENT_LIST_DIR}/${subdir}/CMakeLists.txt)
        message("Adding pattern projection plugin: " ${subdir})
        add_subdirectory(${subdir})
    endif()
endforeach()
